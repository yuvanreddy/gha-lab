# gha-lab - GitHub Actions CI/CD starter

A tiny Python app (app/calculator.py) with pytest tests (tests/). Author your
GitHub Actions workflow YAML under .github/workflows/, commit, and push to your
operator-supplied GitHub repository to trigger runs in the Actions tab.

Build/test locally:  pip install -r requirements.txt  then  pytest -q
